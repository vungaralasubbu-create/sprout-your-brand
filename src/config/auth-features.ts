// Auth feature flags. Toggle to re-enable providers before production launch.
// Google Sign-In is temporarily disabled across the public flow; the underlying
// implementation is preserved so it can be re-enabled by flipping this flag.
export const ENABLE_GOOGLE_AUTH = false;
